apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: ms-cinema
  labels:
    app.kubernetes.io/name: payment-service
    app.kubernetes.io/part-of: ms-cinema
    app.kubernetes.io/component: service
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: payment-service
        app.kubernetes.io/part-of: ms-cinema
    spec:
      initContainers:
        - name: wait-for-postgresql
          image: busybox:1.36
          command: ['sh', '-c']
          args:
            - |
              until nc -z postgresql 5432; do
                echo "Waiting for postgresql..."
                sleep 3
              done
        - name: wait-for-kafka
          image: busybox:1.36
          command: ['sh', '-c']
          args:
            - |
              until nc -z kafka 9092; do
                echo "Waiting for kafka..."
                sleep 3
              done
      containers:
        - name: payment-service
          image: ms-cinema/payment-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: ms-cinema-config
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms64m -Xmx256m -Duser.timezone=Asia/Ho_Chi_Minh"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef: { name: ms-cinema-secrets, key: DB_USERNAME }
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef: { name: ms-cinema-secrets, key: DB_PASSWORD }
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef: { name: ms-cinema-secrets, key: JWT_SECRET }
            - name: STRIPE_API_KEY
              valueFrom:
                secretKeyRef: { name: ms-cinema-secrets, key: STRIPE_API_KEY }
            - name: STRIPE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef: { name: ms-cinema-secrets, key: STRIPE_WEBHOOK_SECRET }
          resources:
            limits: { memory: "512Mi", cpu: "300m" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: ms-cinema
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payment-service
  ports:
    - port: 8084
      targetPort: 8084
